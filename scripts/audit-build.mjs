import assert from "node:assert/strict";import{existsSync}from"node:fs";import{readFile}from"node:fs/promises";import{join,resolve}from"node:path";const root=resolve(import.meta.dirname,"..");const dist=join(root,"dist");const source=await readFile(join(dist,"index.html"),"utf8");const catalog=JSON.parse(await readFile(join(root,"src","data","catalog.json"),"utf8"));assert.equal((source.match(/<h1(?:\s|>)/g)||[]).length,1);assert.equal((source.match(/class="product-card"/g)||[]).length,catalog.length);assert.equal(catalog.length,93);assert.match(source,/6281387927481/);assert.match(source,/class="float-wa"/);assert.doesNotMatch(source,/Event infrastructure partner|Knowledge base|Operational brief/i);for(const item of catalog){assert.equal(existsSync(join(dist,item.image.replace(/^\//,""))),true,item.image)}console.log(`Original-design audit passed: ${catalog.length} RR products rendered.`);