{
  "name": "hrpro-astro",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "node --test tests/catalog.test.mjs tests/quote.test.mjs",
    "test:build": "node --test tests/build-contract.test.mjs",
    "check": "astro check",
    "verify": "npm test && npm run check && npm run build && npm run test:build && node scripts/audit-build.mjs"
  },
  "dependencies": {
    "astro": "^5.17.1"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "@astrojs/sitemap": "^3.6.0",
    "typescript": "^5.9.2"
  }
}
